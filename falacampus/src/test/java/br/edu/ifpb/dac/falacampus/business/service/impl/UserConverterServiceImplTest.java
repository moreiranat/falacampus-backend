package br.edu.ifpb.dac.falacampus.business.service.impl;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import br.edu.ifpb.dac.falacampus.business.service.UserConverterService;
import br.edu.ifpb.dac.falacampus.model.entity.Departament;
import br.edu.ifpb.dac.falacampus.model.entity.User;
import br.edu.ifpb.dac.falacampus.model.enums.Role;
import br.edu.ifpb.dac.falacampus.presentation.dto.UserDto;

class UserConverterServiceImplTest {

	/*
	 * private static UserConverterService userConverterServiceImpl; private static
	 * UserConverterService userConverterService; private static UserDto dto;
	 * private static User user; private static Departament dep;
	 * 
	 * @BeforeAll static void setUpBeforeClass() throws Exception {
	 * 
	 * userConverterServiceImpl = new UserConverterServiceImpl(); user = new User();
	 * dto = new UserDto();
	 * 
	 * dep = new Departament(1L, "Biblioteca", null); userConverterService = new
	 * UserConverterServiceImpl();
	 * 
	 * user.setId(1L); user.setName("Maria"); user.setEmail("maria@email.com");
	 * user.setRegistration(1234L); user.setRole(Role.STUDENT);
	 * user.setPassword("1234"); user.setDepartament(dep);
	 * 
	 * dto.setId(1L); dto.setName("Maria"); dto.setEmail("maria@email.com");
	 * dto.setRegistration(1234L); dto.setRole(Role.STUDENT);
	 * dto.setPassword("1234"); dto.setDepartamentId(dep.getId().longValue()); }
	 * 
	 * @AfterAll static void tearDownAfterClass() throws Exception { }
	 * 
	 * @BeforeEach void setUp() throws Exception {
	 * 
	 * }
	 * 
	 * @AfterEach void tearDown() throws Exception { }
	 * 
	 * @Test void userToDTOTest() {
	 * 
	 * assertEquals(UserDto.class,
	 * userConverterServiceImpl.userToDTO(user).getClass());
	 * 
	 * }
	 * 
	 * @Test void dtoToUserTest() {
	 * 
	 * assertEquals(user.getClass(),
	 * userConverterServiceImpl.dtoToUser(dto).getClass());
	 * 
	 * }
	 * 
	 * @Test public void userToDtoItens() { UserDto userDto =
	 * userConverterService.userToDTO(user); assertAll("userToDto", () ->
	 * assertEquals(userDto.getId(), user.getId()), () ->
	 * assertEquals(userDto.getName(), user.getName()), () ->
	 * assertEquals(userDto.getEmail(), user.getEmail()), () ->
	 * assertEquals(userDto.getRegistration(), user.getRegistration()), () ->
	 * assertEquals(userDto.getRole(), user.getRole()), () ->
	 * assertEquals(userDto.getPassword(), user.getPassword()), () ->
	 * assertEquals(userDto.getDepartamentId().longValue(), user.getDepartament())
	 * ); }
	 * 
	 * @Test public void dtoToUserItens() { User user =
	 * userConverterService.dtoToUser(dto); assertAll("dtoToUser", () ->
	 * assertEquals(user.getId(), dto.getId()), () -> assertEquals(user.getName(),
	 * dto.getName()), () -> assertEquals(user.getEmail(), dto.getEmail()), () ->
	 * assertEquals(user.getRegistration(), dto.getRegistration()), () ->
	 * assertEquals(user.getRole(), dto.getRole()), () ->
	 * assertEquals(user.getPassword(), dto.getPassword()), () ->
	 * assertEquals(user.getDepartament(), dto.getDepartamentId()) ); }
	 */

}
